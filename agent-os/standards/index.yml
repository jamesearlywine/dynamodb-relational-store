# Agent OS Standards Index

factories:
  create-options-and-flow:
    description: Create*Options interface and factory flow (validate, keys, timestamps)

global:
  public-api-and-errors:
    description: Explicit return types, JSDoc, descriptive throws, immutable record IDs

testing:
  vitest-co-located:
    description: Vitest, co-located *.test.ts, and what to test (factories, validation, type guards)

types:
  record-discriminant-and-fields:
    description: _recordType discriminant and underscore-prefixed metadata fields

validation:
  type-guards:
    description: Type guards use _recordType then Zod schema safeParse
  urn-and-zod:
    description: validateUrn before use, Zod schema co-located with factory, schema helpers from utils
