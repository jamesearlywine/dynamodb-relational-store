/**
 * DynamoDB attribute value types
 *
 * Type definitions for DynamoDB attribute values and related utilities.
 * These types may be extended as needed for DynamoDB client integration.
 */

/**
 * DynamoDB attribute value type.
 * This can be extended to match the AWS SDK DynamoDB AttributeValue type.
 */
export type DynamoDBAttributeValue = unknown;

